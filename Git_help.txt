You always need to commit changes
git clone https://git-lab/beispiel.de % Kopiere Link von Git-Hub bzw. Git-Lab
git commit -a -m "Kommentar zum commit" % -a -> all -m ->message Nachricht muss in Gänsefüchßen stehen
git status % Zeigt den Git Status an 

